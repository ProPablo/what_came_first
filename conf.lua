-- THis file will load the default configuration for the game
function love.conf(t)
    t.window.width = 800
    t.window.height = 400
    t.console = true  -- Enable console for debugging (this is just std:out)
end